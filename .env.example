# 복사해서 사용: `cp .env.example .env` 후 실제 값을 채우세요.
# 실제 .env 는 .gitignore 로 커밋되지 않습니다. 토큰을 여기(예시 파일)에 넣지 마세요.

# Figma Personal Access Token (figd_ 로 시작)
FIGMA_API_KEY=

# Notion internal integration token (ntn_ 으로 시작)
NOTION_TOKEN=
