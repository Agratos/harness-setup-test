# 의존성·빌드 산출물
node_modules/
dist/
*.tsbuildinfo
.yarn/cache/
.yarn/install-state.gz
.yarn/unplugged/

# 환경 변수 (Notion 토큰 등)
.env

# 에디터·세션 부산물
.omc/
*.local

# 하네스 런타임 상태 (페이즈마다 변동 — 추적 제외; merge 체크아웃 충돌 방지)
harness/state.json

# 하네스 사이클 로그(페이즈마다 변동 — merge 체크아웃 충돌 방지, state.json 과 동일 성격)
harness/cycles/cycle-log.ndjson
