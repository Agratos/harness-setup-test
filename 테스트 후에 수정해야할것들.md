# 테스트 후에 수정해야 할 것들

> 반복 루프 백로그. 회차마다: 테스트 중 발견 → 여기 기록 → 하네스에 반영 → 이 내용 비움 → 다음 테스트.
> **4차 테스트(habit-tracker) 진행 중** — 발견 사항을 아래에 적는다.

## A. 하네스 버그 / 개선 (코드/문서)

- **(4차 발견, 사용자 지적) ✅ 반영완료 — 🧪 테스트 관리 페이지에 캡처 사진이 안 보임 → File Upload API 로 행 본문에 첨부**: 캡처 PNG 는 로컬에만 저장되고 MCP 커넥터는 파일 업로드 불가라 행만 있고 사진이 비어 보였음. 반영: ① `lib/notion-api.mjs` 에 `uploadFile`(`POST /v1/file_uploads` single_part → multipart send)·`attachStoryboard`(멱등 마커 callout + image 블록)·순수빌더(`storyboardItems/Caption/Marker`, `imageBlocks`) 추가, ② 신규 CLI `scripts/notion-storyboard.mjs --id=<scen> --row=<행id>`(yarn `notion:storyboard`) — 행 생성은 MCP, 업로드+첨부만 REST 로 분업, ③ `notion-storyboard.selftest`(15) + CI 등록(12종), ④ 문서: hub-layout §4(첨부 규약)·§8(File Upload API 행)·qa.md·run-cycle.md §verify. **실증**: habit-tracker 허브 🧪 테스트 관리 행에 story-demo 12컷 첨부 → callout1+image12 전부 S3 url 렌더, 재실행 skip(멱등) 확인.
- **(4차 발견) eval-scenario 러너가 Mantine SegmentedControl 을 못 누름**: `clickText` 가 텍스트→radio(force) 다 시도해도 SegmentedControl 의 radio input 이 "viewport 밖 hidden" 이라 클릭 실패. 앱 버그 아님(필터 로직은 단위테스트·B3 로 검증됨). → Fix 후보: SegmentedControl 은 **보이는 label/span 을 클릭**(getByText 로 label 잡아 클릭, 또는 `[role=radiogroup] label:has-text()`). 탭/세그먼트 위젯 일반 지원으로.
- **(4차, C 통합버그 — 이미 수정)** eval-scenario 결과 JSON 이 done-gate 평가로 오인 → `<id>/scenario.json` 서브폴더 + done-gate `eval-*.json` 필터로 수정(커밋 `014de47`). ✅

## C. ✅ 반영완료 — QA 상호작용/E2E 검증 (사용자 지적, 최대 갭)
> 반영: `scripts/eval-scenario.mjs`(+selftest 12/12) · `agents/qa.md`·`run-cycle.md` 의무화 · `harness/eval-scenario.example.json` · `yarn eval:scenario`. **실증(reading-log)**: 러너가 폼버그(추가 후 미초기화)를 `inputEmpty` 단언으로 적발 → `form.key` 수정 → 재실행 PASS. (정적·단위는 못 잡던 것)
- **증상(실제 버그)**: reading-log `AddBookForm` 이 `mode:'uncontrolled'` 인데 입력에 `key={form.key('필드')}` 누락 → 추가 후 제목·저자 안 비워지고, 상태 바꿔 재추가 시 적용 안 됨. **사용자가 직접 써보고 발견.**
- **왜 통과됐나**: 결정적 게이트 = store.addBook 단위테스트(폼 상호작용 X) / B3 = 정적 스크린샷(초기 렌더만) / eval-playwright = 렌더·마운트만. **아무도 입력→추가→재추가를 실제로 안 해봄.** 현재 `qa` 에이전트도 typecheck/lint/test/check-arch(단위·정적)만 → "QA 가 일을 안 함".
- **Fix — 상호작용(E2E) 검증 에이전트/러너**:
  1. `scripts/eval-scenario.mjs` (Playwright 상호작용 러너): 시나리오 스펙(액션 `fill`/`select`/`setRating`/`click` + 단언 `카드 수`/`폼 입력값 비워짐`/`배지 텍스트`/`통계 값`)을 dev 서버에서 **실제 실행**, 단계별 스크린샷, 실패 단언 → 기능 불만(major) → done-gate fail → rework.
  2. 시나리오는 decompose/페르소나에서 도출. 예(이 버그 잡음): `제목·저자·상태=예정·평점 입력 → 추가 → [카드 1개 + 폼 입력 비워짐 단언] → 상태=완독 → 추가 → [카드 2개 + 2번째 배지=완독 + 통계 갱신 단언]`.
  3. `qa` 를 "결정적 게이트 + 핵심 시나리오 실제 실행·단언"으로 확장(또는 신규 `qa-e2e`). `run-cycle.md` evaluate/verify 에 "정적 렌더만으로 통과 금지, 핵심 유스케이스를 실제 조작·단언" 의무화. `.claude/agents/qa.md`·`customer.md` 갱신.

## D. ✅ 반영완료 — Team Roster 한글 역할 설명 (사용자 지적)
> 반영: Roster 9개 행 `메모` 에 한글 역할 설명 채움 + 기본 뷰에 메모 컬럼 표시(라이브). `hub-layout §4` 문서 반영.
- 👥 Team Roster DB 의 9개 에이전트(ceo·pm·architect·entity-modeler·ui·ux·qa·quality·customer)에 **`메모` 필드(또는 설명 컬럼)로 역할을 한글 한 줄 설명** 채우기. 현재 역할/스킬은 영문·태그뿐이라 한눈에 안 들어옴. `docs/notion-hub-layout.md §4 Team Roster` 에 한글 메모 규약 명시.

## B. 시뮬레이션 품질 / 프로세스

- **B(3차 발견, minor) 스캐폴드 헤더 정렬**: 본문은 중앙 Container+padding 으로 양호하나, 상단 스캐폴드 헤더(h1 `<project>`+부제, App.tsx)가 컨테이너 밖 좌측정렬이라 본문과 어긋남. B3 캡처 실측으로 적발(루브릭 100이 놓침). → Fix 후보: 템플릿 `src/app/App.tsx` 의 헤더도 본문과 동일 `Container` 안에 배치(또는 ui.md 에 "h1 포함 전체를 컨테이너에" 명시). 출시 차단 아님(minor).

---
완료분(상세는 git 이력 — 커밋 메시지 참조):
- **1차 6건**: Notion no-op · state.json 추적제외 · 구현 commit-before-merge · default branch=main · eval 이름 하드코딩 · --init 자동전진
- **2차 5건**: B3 캡처물 소비 평가 · A1 콜아웃 상태줄(설명+상태줄 자식, 매 사이클 갱신) · A2 Cycles 기본뷰 평가 컬럼 · A3 teardown killByPort 폴백 · A4 cycle-log 추적제외
